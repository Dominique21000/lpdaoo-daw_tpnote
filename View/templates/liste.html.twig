{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}
   
{% block content %}
    <div class="row">
    <h1>Liste des cochons</h1>
    </div>
    <div class="row">
    {% for c in cochons %}
        <div class="col-lg-3 col-md-4 col-sm-12">
            <div class="card" style="width: 18rem;">
                <img class="card-img-top" src="public/img/photo/{{ c.pho_fichier }}" alt="Cochon : {{ c.coc_nom }}" style="height:200px" >
                <div class="card-body">
                    <h5 class="card-title">{{ c.coc_nom}}</h5>
                    <p class="card-text">{{ c.coc_description | truncate(10) }}</p>
                    <a href="index.php?rub=contact" class="btn btn-primary">Contact</a>
                    <a href="index.php?rub=details&id={{c.coc_id}}" class="btn btn-primary">Détails</a>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>
{% endblock %}